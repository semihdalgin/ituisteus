<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".BasincActivty" >

    <TextView
        android:id="@+id/atmLabelTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Atm: " />
    
    <TextView android:id="@+id/atmLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/atmLabelTitle"
        android:layout_alignBottom="@id/atmLabelTitle"
        android:layout_toRightOf="@id/atmLabelTitle"
        android:text="@string/notAvailable"/>
    
    <TextView android:id="@+id/atmLabelT2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/atmLabel"
        android:layout_alignBottom="@id/atmLabel"
        android:layout_toRightOf="@id/atmLabel"
        android:text=" hPa"/>
    
    <TextView android:id="@+id/SectionDivider1"
        style="@style/line_separator"
        android:layout_below="@id/atmLabelT2" />
    
    <TextView
        android:id="@+id/kotTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/SectionDivider1"
        android:text="Kot: " />
    
      <TextView android:id="@+id/kotLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/kotTitle"
        android:layout_alignBottom="@id/kotTitle"
        android:layout_toRightOf="@id/kotTitle"
        android:text="@string/notAvailable"/>
      
      <TextView android:id="@+id/kotLabelT2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/kotLabel"
        android:layout_alignBottom="@id/kotLabel"
        android:layout_toRightOf="@id/kotLabel"
        android:text=" m"/>
    
   

</RelativeLayout>
